<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zcr.mymodulproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:name="com.jg.mymodulproject.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/commonAppTheme"
        tools:replace="android:label">
        <activity android:name="com.jg.mymodulproject.WelcomeActitivy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.jg.mymodulproject.login.LoginActivity"/>
        <activity android:name="com.jg.mymodulproject.home.HomeActivity"/>
        <activity android:name="com.jg.mymodulproject.sampling.SamplingActivity" />
        <activity android:name="com.jg.mymodulproject.sampling.SamplingDetailActivity" />
    </application>

</manifest>