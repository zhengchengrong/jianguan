<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.luojilab.reader">

    <application
        android:name=".runalone.application.ReaderApplication"
        android:allowBackup="true"
        android:label="@string/readerbook_app_name"
        android:supportsRtl="true"
        tools:replace="android:label"
        android:theme="@style/commonAppTheme"
        >

        <activity android:name=".runalone.ReaderTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
